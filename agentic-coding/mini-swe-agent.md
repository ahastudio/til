# mini-SWE-agent: 100줄짜리 AI 코딩 에이전트

원문: <https://minimal-agent.com/>

GitHub: <https://github.com/SWE-agent/mini-swe-agent>

## 요약

Princeton & Stanford 연구팀이 공개한 튜토리얼로, AI 에이전트를 50~60줄 남짓의
코드로 구현하는 방법을 설명한다. 핵심 주장은 단순하다: 충분히 강력한 언어 모델이
있다면, 정교한 도구 세트나 복잡한 제어 흐름 없이도 SWE-bench Verified에서 74%
이상의 점수를 달성할 수 있다는 것이다.

에이전트의 구조는 단일 루프다. 언어 모델에 현재 상태를 전달하고, 모델이 제안하는
액션(bash 명령)을 파싱하고, `subprocess.run`으로 실행하고, 출력 결과를 다시
모델에게 돌려준다. 이 사이클을 반복하면 에이전트가 완성된다. 마크다운 코드 블록이나
XML 태그로 인코딩된 명령을 정규식으로 추출하는 것이 전부다.

도구 호출(tool calling) API를 전혀 사용하지 않는다는 점이 핵심 설계 결정이다.
모델이 bash를 도구로 삼아 직접 셸 명령을 내리도록 한다. 파일 읽기, 검색, 테스트
실행, PR 생성 — 모든 것을 bash로 해결한다. 특정 작업마다 전용 도구를 구현하는
대신, 언어 모델이 셸을 최대한 활용하도록 맡긴다.

실행 방식도 의도적으로 단순하다. 스테이트풀(stateful) 셸 세션을 유지하는 대신,
매 액션을 독립적인 `subprocess.run` 호출로 처리한다. 덕분에 샌드박스 전환이
간단해진다. 로컬에서 `subprocess.run`이던 것을 `docker exec`로 바꾸기만 하면
컨테이너 환경으로 이식된다. 히스토리는 완전히 선형이다. 매 스텝마다 메시지를
누적하는 것이 전부이므로, 디버깅과 파인튜닝 데이터 생성에 적합하다.

프로덕션 구현인 `mini-swe-agent`는 이 원리를 확장하여 로컬·Docker/Podman·
Singularity 등 다양한 실행 환경을 지원하며, litellm을 통해 사실상 모든 모델과
호환된다. Meta, NVIDIA, IBM, Princeton, Stanford 등 다수의 조직이 이미 사용 중이다.

## 분석

### “도구를 줄이면 성능이 올라간다”는 역설

2024년 초 SWE-agent가 처음 등장했을 때, 연구팀은 언어 모델에게 파일 편집,
검색, 탐색 등 전용 도구를 제공하는 데 많은 공을 들였다. 그런데 1년 후, 같은
팀이 도구를 bash 하나로 줄인 버전이 거의 동등한 성능을 낸다는 것을 보여준다.
이는 모델 역량이 향상됨에 따라 “스캐폴드(scaffold)의 복잡도”가 성능의 결정 인자에서
멀어지고 있음을 시사한다. 2024년에 필요했던 도구들이 2025년에는 불필요한
오버헤드가 된 것이다.

### subprocess.run의 건축적 함의

매 액션을 독립적인 프로세스로 실행하는 설계는 단순함 이상의 의미를 갖는다.
상태가 없으므로(stateless) 각 명령은 완전히 격리된다. 환경 변수 오염, 예상치
못한 셸 상태, 장시간 실행 프로세스가 남긴 부작용이 다음 액션에 영향을 미치지
않는다. 이것이 샌드박스 전환을 `subprocess.run` → `docker exec` 한 줄 교체로
끝낼 수 있는 근거다. 에이전트 실행 환경을 수평 확장할 때도 각 액션이 독립적이므로
병렬화가 자연스럽다.

### 선형 히스토리와 파인튜닝

히스토리가 완전히 선형이라는 특성은 파인튜닝(fine-tuning)과 강화 학습(RL)에서
중요한 장점이다. 에이전트 궤적(trajectory)과 언어 모델에 전달되는 메시지가 동일하므로,
학습 데이터 생성 과정에서 별도의 변환 작업이 없다. 에이전트 행동을 모델 학습에
직접 활용할 수 있다는 의미다. SWE-agent가 특정 스캐폴드에 과적합(overfitting)되는
문제를 피하고 싶을 때 mini를 베이스라인으로 쓰라고 권하는 이유가 여기에 있다.

### SWE-bench 생태계와의 연결

SWE-bench를 만든 팀이 SWE-agent를 만들고, 이제 mini-swe-agent를 만들었다.
벤치마크 제작자가 직접 에이전트를 개발한다는 사실은 벤치마크-에이전트 공진화(co-evolution)
구조를 만든다. mini-swe-agent는 SWE-bench에서 모델 성능을 비교하는 표준 실행
환경으로 자리잡고 있으며, 리더보드에 “bash only” 조건을 명시하여 스캐폴드 변수를
통제한다. 이는 벤치마크의 신뢰성을 높이는 동시에 mini의 채택을 촉진하는 선순환이다.

## 비평

### 강점

가장 큰 강점은 교육적 명료함이다. 50줄짜리 구현이 74% SWE-bench 점수를 낸다는
사실은 “에이전트 = 복잡한 시스템”이라는 오해를 깨는 강력한 증거다. 연구자와
실무자 모두 이 구현을 읽고 에이전트의 본질을 이해할 수 있다. 코드가 곧 설명서인
드문 프로젝트다.

실행 환경 이식성도 탁월하다. 로컬, Docker, Singularity, bubblewrap, contree 등
다양한 샌드박스를 지원하는 구조가 설계 수준에서 내장되어 있다. 실험 환경 구축의
마찰을 최소화한다.

### 약점 및 한계

bash 단일 도구 접근법은 장점이자 한계다. 언어 모델이 bash를 충분히 잘 다루지
못하는 작업 영역(GUI 조작, 특수 파일 형식 파싱 등)에서는 성능이 급격히 저하될 수
있다. 74% SWE-bench 점수는 특정 모델(Gemini 3 Pro)과 특정 벤치마크 도메인(GitHub
이슈 해결)의 조합에서 나온 수치임을 감안해야 한다. 다른 도메인에서의 일반화 여부는
별도의 검증이 필요하다.

`subprocess.run` 기반의 독립 실행 방식은 액션 간 상태를 공유해야 하는 작업에서
불편함을 낳는다. 예를 들어 가상환경을 활성화하거나, 특정 디렉터리에 머물거나,
환경 변수를 영속적으로 설정하는 것이 각 액션마다 반복되어야 한다. 이를 환경 변수
관리로 우회하지만, 복잡한 상태를 요하는 작업에서는 한계가 드러날 수 있다.

튜토리얼 사이트(minimal-agent.com) 자체는 내용이 간결한 반면, 실제 프로덕션
구현(mini-swe-agent)은 문서가 분산되어 있어 전체 그림을 파악하는 데 시간이 걸린다.
입문자에게 “50줄”을 보여주고 실제 사용은 훨씬 복잡한 구현으로 안내하는 구조가
일부 기대 불일치를 만들 수 있다.

## 인사이트

### 에이전트 설계의 무게중심이 스캐폴드에서 모델로 이동하고 있다

2023~2024년의 에이전트 연구는 “어떤 도구를 줄 것인가”, “히스토리를 어떻게 압축할
것인가”, “어떤 인터페이스를 설계할 것인가”에 집중했다. 에이전트 스캐폴드가
성능의 주된 결정 변수였기 때문이다. mini-swe-agent는 이 전제가 2025년에는
더 이상 유효하지 않음을 보여주는 증거다.

bash 하나로 74%를 달성한다는 사실은 모델 자체의 추론 능력이 스캐폴드 최적화보다
훨씬 더 중요해졌음을 의미한다. 에이전트 엔지니어링의 한계 수익이 줄어들고 있는
반면, 모델 개선의 한계 수익은 여전히 높다. 이는 에이전트 연구의 방향이 “더 좋은
도구 설계”에서 “더 좋은 모델을 어떻게 활용하는가”로 재편되고 있음을 시사한다.

두 번째 함의는 에이전트 프레임워크 시장에 관한 것이다. LangChain, AutoGen,
CrewAI 같은 프레임워크들은 복잡한 도구 체인과 에이전트 오케스트레이션을 강조한다.
모델이 충분히 강력해질수록 이 복잡성의 가치는 특수한 사용 사례로 좁아진다.
범용 코딩 에이전트 영역에서는 단순한 루프가 정답일 수 있다. 도구 생태계가
복잡해질수록, 오히려 가장 단순한 인터페이스(bash)가 가장 유연한 인터페이스가
된다는 역설이 실현되고 있다.

### 벤치마크-에이전트 공진화가 만드는 측정의 함정

SWE-bench를 만든 팀이 SWE-agent를 만들고, 다시 mini-swe-agent를 만들었다.
이 팀이 벤치마크의 출제자이자 상위 응시자라는 구조는 검토가 필요한 이해 충돌이다.
mini-swe-agent가 SWE-bench에서 높은 점수를 내는 것은 당연히 좋은 결과지만,
그것이 “실제 소프트웨어 엔지니어링 역량”을 측정하는지, 아니면 “SWE-bench에
최적화된 역량”을 측정하는지를 구분하기 어렵다.

더 넓은 맥락에서 보면, 현재 AI 코딩 에이전트 평가 생태계는 소수의 팀이 벤치마크와
에이전트를 동시에 개발하는 구조로 집중되어 있다. 이것이 연구 속도를 높이는 장점이
있지만, 벤치마크가 특정 접근법에 유리하게 설계되는 경향을 만들 수 있다.
SWE-bench가 bash 친화적인 작업 중심으로 구성되어 있다면, bash 단일 도구 접근법이
과대평가될 여지가 있다.

이 문제는 mini-swe-agent만의 것이 아니라 AI 벤치마크 전반의 구조적 도전이다.
평가 도구와 피평가 시스템을 분리하는 독립성의 원칙이 AI 연구 생태계에서도
점점 중요해지고 있다. mini-swe-agent의 성과를 제대로 이해하려면 SWE-bench 외의
다양한 벤치마크에서의 검증이 필요하다.

### “100줄 신화”가 실제로 말해주는 것

100줄짜리 에이전트가 대형 에이전트 프레임워크에 근접한 성능을 낸다는 사실이
“에이전트는 간단하다”는 결론으로 이어지면 안 된다. 오히려 정반대의 통찰이 맞다.

100줄이 작동하는 것은 그 뒤에 수조 개의 파라미터로 훈련된 언어 모델, 수백억
달러의 연산 인프라, 수년간의 RLHF와 파인튜닝이 있기 때문이다. 에이전트 코드의
간결함은 모델의 복잡성 위에 서 있다. “단순한 에이전트”라는 표현은 스캐폴드가
단순하다는 것이지, 시스템 전체가 단순하다는 것이 아니다.

이 구분이 중요한 이유가 있다. “100줄로 ChatGPT 수준의 코딩 에이전트를 만들 수
있다”는 메시지는 사실이지만, “100줄이면 충분하다”는 메시지는 위험하다.
전자는 에이전트 설계의 접근성을 높이고, 후자는 모델 선택·실행 환경·안전성 설계의
중요성을 과소평가하게 만든다. mini-swe-agent 튜토리얼이 암묵적으로 전달하는
메시지와 독자가 받아들이는 메시지 사이에 이 간극이 존재한다.

에이전트 시대의 진입 장벽이 낮아졌다는 것은 분명한 사실이다. 하지만 그 낮은
장벽 뒤에 있는 시스템(모델, 인프라, 안전 설계)의 복잡성은 전혀 줄어들지 않았다.
100줄의 코드가 보여주는 것은 에이전트의 단순함이 아니라, 복잡성의 위치가 바뀌었다는 것이다.
