codex exec "
  다음 소스에서 최근 48시간 이내에 올라온 개발자 트렌드 주제 10개를 찾아줘:
  - Hacker News (top/new)
  - GitHub Trending (오늘 기준)

  선정 기준:
  - 조회수/댓글/스타 수 등 실제 반응이 많은 것 우선
  - 실용적으로 바로 써먹을 수 있는 것 우선
  - 단순 뉴스보다 기술적 깊이가 있는 것 우선

  중복 방지 규칙 (반드시 준수):
  - sandbox.md 파일의 기존 내용을 먼저 읽어서 이미 등장한 항목을 파악해.
  - 기존 섹션에 이미 포함된 URL과 동일한 URL은 절대 추가하지 마.
  - 기존 섹션에 이미 등장한 프로젝트명·기사 제목과 동일하거나 실질적으로 같은
    항목은 절대 추가하지 마.
  - 후보가 10개에 못 미치더라도 중복 항목을 추가하지 말고, 새로운 항목만 채워.

  sandbox.md 파일에 오늘 날짜(YYYY-MM-DD)와 트렌드 주제를 조합한
  '## YYYY-MM-DD 개발자 트렌드' 형식의 h2 섹션을 새로 추가하고,
  그 아래에 각 항목을 다음 형식으로 작성해 줘:

  ### {번호}. {제목}

  - **출처**: {사이트명} — <{URL}>
  - **한 줄 요약**: {핵심 내용을 한 문장으로}
  - **왜 주목받나**: {커뮤니티 반응과 인기 이유}
  - **개발자 관점 인사이트**:
    - 이 기술/이슈가 실무에 어떤 영향을 주는지
    - 지금 당장 써먹을 수 있다면 어떻게 활용할 수 있는지
    - 앞으로 어떤 방향으로 흘러갈 것 같은지 (트렌드 예측)
    - 놓치면 안 되는 핵심 포인트나 주의사항

  출력 형식 강제 규칙 (반드시 준수):
  - '- **개발자 관점 인사이트**:' 뒤에는 반드시 하위 불릿 4개를 줄바꿈으로 작성해.
  - 네 개의 하위 불릿은 반드시 다음 라벨로 시작해:
    1) '이 기술/이슈가 실무에 어떤 영향을 주는지:'
    2) '지금 당장 써먹을 수 있다면 어떻게 활용할 수 있는지:'
    3) '앞으로 어떤 방향으로 흘러갈 것 같은지 (트렌드 예측):'
    4) '놓치면 안 되는 핵심 포인트나 주의사항:'
  - '실무 영향: ... 즉시 활용: ... 방향: ... 주의: ...'처럼 한 줄 문단으로 합치지 마.
  - 각 하위 불릿은 한 문장 이상으로 구체적으로 작성해.
"

npx prettier --write sandbox.md

git add sandbox.md

codex exec '
  git diff --staged 내용을 기반으로 Git Commit Message를 작성하고,
  /tmp/til-update-sandbox-commit-message.txt 파일에 저장해줘.
'

git commit -F /tmp/til-update-sandbox-commit-message.txt

git fetch origin --prune

git pull --rebase

git push
