# 씨앗 1.0: 고1 학생이 프리트레인부터 직접 만든 254.7M 한국어 전용 모델

Show GN: [ssiat llm - Pi Zero 2W에서도 동작가능한 한국어 LLM](https://news.hada.io/topic?id=33581)

## 요약

중학생 때부터 혼자 LLM을 공부해 온 고등학교 1학년 학생 mogodik이 GeekNews에 올린 Show GN 글이다.
파인튜닝이 아니라 프리트레인부터 직접 빌드한 254.7M 파라미터의 한국어 전용 모델 씨앗(Ssiat) 1.0을 공개했다.
자원이 부족한 학생이나 개인 개발자도 LLM을 직접 다뤄 볼 수 있게 하고,
비용 부담 없이 캐릭터 챗을 만들 수 있는 모델을 만드는 것이 목표라고 밝힌다.
글은 8포인트를 받았고 댓글은 두 개가 달렸다.

모델은 Llama 아키텍처를 따르며 Apache 2.0으로 배포된다.
이론상 256MB RAM과 CPU만 있으면 구동 가능하고, 양자화 기준으로 Pi Zero 2W와 갤럭시 워치 4에서 동작을 확인했다고 적는다.
캐릭터 하나를 학습시키는 데 예시 50~80개가 필요하고, 5060 Ti 16GB 기준 LoRA로 1~2분이 걸린다.
차별점으로 내세우는 것은 두 가지다.
크기가 작아 데이터 수정과 재학습과 확인을 하루에도 여러 번 반복할 수 있다는 점,
그리고 지식량보다 캐릭터 유지력에 강점이 있어 예시 800개에서 1000개 안팎이면 10턴 이상의 대화에서도 캐릭터가 무너지지 않는다는 점이다.

학습 정보는 구체적으로 공개되어 있다.
프리트레인 스텝은 781,000이고, 배치 구성은 batch 8에 grad_accum 2에 block_size 512로 스텝당 8,192 토큰이다.
곱하면 총 학습 토큰은 약 6.4B이다.
학습 속도는 A100 기준 초당 약 470.25 토큰이라고 모델 카드에 적혀 있다.
SFT는 되어 있으나 모델 자체는 정체성을 갖고 있지 않다.
이름도 성격도 말투도 없는 백지 상태이며, 캐릭터성은 의도적으로 제외했다는 설명이다.

모델 카드가 공개한 설정 파일을 보면 구조가 더 분명해진다.
은닉 차원 1024, 레이어 16개, 어텐션 헤드 16개이며 키·밸류 헤드도 16개로 그룹 쿼리 어텐션을 쓰지 않는다.
MLP 중간 차원은 2816, 어휘 크기는 48,000, 최대 위치 임베딩은 1024다.
입출력 임베딩은 묶여 있고 RoPE theta는 10000, 가중치는 float32로 올라가 있다.
대화 포맷은 `<|im_start|>` 와 `<|im_end|>` 를 쓰는 ChatML 계열이고,
권장 설정은 temperature 0.3~0.5에 repeat_penalty 1.3이다.

예시 캐릭터가 둘 포함되어 있다.
학습 데이터 801개로 만든 ERROR는 기억을 전부 잃은 정체 불명의 존재로, 일상적인 질문에도 회피하거나 철학적으로 답한다.
데이터 60개로 만든 씨앗은 방금 태어난 호기심 많은 AI로, 자기 이름과 수학·영어를 못한다는 한계를 스스로 인정한다.
둘 다 LoRA r=64로 학습했고 GGUF 파일이 함께 올라와 있다.
모델 카드는 예시 30개면 말투와 성격이 잡히고, 50개를 넘기면서 확실히 안정된다고 적는다.

한계도 함께 밝힌다.
세계 지식과 사실 정확도가 약하고, 영어를 하지 못하며 수학과 계산도 다루지 않는다.
한국어를 제외한 언어가 학습 데이터셋에 거의 없기 때문이다.
긴 대화에서 앞서 나온 사실을 정확히 기억하는 능력도 제한적이다.
운영상의 문제로는 Ollama 실행 시 버그가 있다.
Ollama가 포크해 쓰는 자체 추론 엔진이 이 모델의 pre-tokenizer를 gpt-2 폴백으로 처리하면서 프롬프트를 잘못 인코딩해
대부분 깨지거나 엉뚱한 답이 나오며, llama.cpp로 직접 실행하면 문제가 없다.
작성자는 이 호환 이슈의 해결 방법에 대한 의견을 구한다고 적었다.

댓글은 둘이다.
parkjy는 고1이 이 정도면 대단하다며 학교에서 세부능력 및 특기사항으로 쓸 수 있지 않겠느냐고 물었다.[^parkjy]
작성자는 성적 문제로 자연계를 가기 어려울 것 같아 실질적 도움이 될지는 의문이지만
포트폴리오로서는 만족스러운 결과물이라고 답했다.[^mogodik]

## 분석

### 모델을 지식 장치가 아니라 문체 장치로 재정의했다

이 프로젝트의 가장 중요한 결정은 크기가 아니라 목표 설정이다.
254.7M 모델로 무엇을 할 수 있느냐는 질문에 대해, 작성자는 질문에 답하는 능력이 아니라 캐릭터를 유지하는 능력을 골랐다.
모델 카드가 이 모델의 강점은 지식의 양이 아니라 캐릭터를 끝까지 유지하는 능력이라고 명시한 대목이 그것이다.

이 선택이 기술적으로 합리적인 이유가 있다.
사실 지식은 파라미터에 저장되는 정보량에 거의 비례하므로 작은 모델에서 먼저 무너지는 항목이다.
반면 말투와 어조와 문장 구조의 일관성은 훨씬 적은 용량으로도 학습된다.
같은 규모에서 지식을 겨루면 확실히 지고 문체를 겨루면 해볼 만하다는 판단이며,
캐릭터 챗이라는 용도는 그 판단이 그대로 제품 가치가 되는 몇 안 되는 영역이다.

정체성을 의도적으로 제외한 것도 같은 논리의 연장이다.
기본 모델이 이름과 성격을 갖고 있으면 그 위에 다른 캐릭터를 얹을 때 원래 성격이 배어 나온다.
백지 상태로 두면 50개에서 80개의 예시만으로 원하는 인격을 덮어씌울 수 있고,
이것이 하루에 여러 번 반복 가능한 학습 주기와 결합해 실습 도구로서의 성격을 만든다.

### 공개된 숫자들이 서로 맞물린다

이 모델 카드에서 드물게 좋은 점은 주장과 수치가 검산 가능하다는 것이다.

파라미터 구성을 설정 파일로 계산해 보면 실제로 254.7M이 나온다.
어휘 48,000에 은닉 차원 1024를 곱하면 임베딩이 약 49.2M이고, 입출력이 묶여 있으므로 한 번만 센다.
레이어 하나는 어텐션 4개 행렬이 약 4.19M, MLP 세 행렬이 약 8.65M으로 합쳐 약 12.8M이고,
16개 레이어면 약 205.5M이다.
둘을 더하면 254.7M에 정확히 도달한다.

학습량도 마찬가지다.
781,000 스텝에 스텝당 8,192 토큰이면 약 6.4B 토큰이고, 파라미터당 약 25 토큰이다.
Chinchilla 계열의 경험칙이 제시하는 파라미터당 20 토큰을 조금 넘는 수준이므로,
계산 자원 대비 학습량이 대략 균형점 근처에 있다는 뜻이다.
혼자 공부한 고1 학생의 프로젝트에서 이 균형이 맞아떨어진 것은 우연이라기보다 참고한 자료를 제대로 소화했다는 신호로 읽힌다.

### 배포 경로가 실행 환경의 현실을 드러낸다

모델 카드가 가장 눈에 띄는 자리에 적어 둔 것이 Ollama에서 깨진다는 경고다.
Ollama가 포크해 쓰는 추론 엔진이 pre-tokenizer를 gpt-2 폴백으로 처리하면서 프롬프트 인코딩이 어긋난다는 진단까지 함께 적혀 있다.

이 문제가 이 프로젝트에서 특히 아픈 이유가 있다.
목표 사용자가 자원이 부족한 학생과 개인 개발자인데, 그 집단이 로컬 모델을 실행하는 가장 흔한 경로가 Ollama다.
llama.cpp로 직접 실행하면 된다는 우회는 기술적으로 맞지만,
명령줄에서 서버를 띄우고 포트를 지정하는 일을 감당할 수 있는 사용자는 이미 진입 장벽을 넘은 사람들이다.
접근성을 목표로 삼은 프로젝트가 접근성이 가장 높은 실행 경로에서 깨진다는 것이 이 배포의 핵심 문제다.

원인의 성격도 시사적이다.
모델 자체의 결함이 아니라 자체 토크나이저를 쓰는 소형 모델이 기성 런타임의 폴백 경로에 걸린 사례다.
직접 프리트레인한 모델은 기존 어휘를 물려받지 않으므로 이런 충돌을 만나기 쉽고,
그것이 처음부터 만든다는 선택에 따라오는 숨은 비용이다.

### 댓글 두 개가 이 프로젝트의 사회적 맥락을 드러낸다

기술 게시판의 반응으로는 특이한 대화가 오갔다.
첫 댓글은 결과물을 칭찬한 뒤 곧바로 이것을 생활기록부의 세부능력 및 특기사항으로 쓸 수 있지 않겠느냐고 묻는다.[^parkjy]
작성자의 답은 성적 문제로 자연계 진학이 어려울 것 같아 실질적 도움이 될지 의문이지만 포트폴리오로는 만족한다는 것이다.[^mogodik]

짧은 교환이지만 한국에서 이런 결과물이 놓이는 자리를 정확히 보여 준다.
6.4B 토큰을 학습시켜 254.7M 모델을 만들어 낸 성과가 먼저 평가받는 축이 입시이고,
그 축에서는 성적이라는 다른 변수가 결과를 좌우한다.
포트폴리오라는 단어를 쓴 것은 입시 바깥의 평가 경로를 스스로 상정한 것이며,
그 경로가 실제로 작동하는지는 이 학생이 아니라 이 분야의 채용과 교육이 답해야 할 문제다.

## 비평

### 컨텍스트 1024가 핵심 주장을 거의 그대로 설명한다

이 모델이 내세우는 강점은 10턴 이상의 대화에서도 캐릭터가 무너지지 않는다는 것이고,
밝힌 한계는 긴 대화에서 앞서 나온 사실을 정확히 기억하는 능력이 제한적이라는 것이다.
두 진술이 나란히 놓여 있지만, 설정 파일의 최대 위치 임베딩이 1024라는 사실이 둘을 함께 설명한다.

1024 토큰은 시스템 프롬프트와 대화 전체를 담아야 하는 총량이다.
한 번의 주고받기가 50 토큰이라고 잡아도 10턴이면 1,000 토큰에 이르므로, 주장된 10턴은 창의 거의 끝이다.
그 지점을 넘으면 앞부분이 창 밖으로 밀려나므로 이전 사실을 기억하지 못하는 것이 당연하고,
이것은 모델 용량의 한계가 아니라 창 크기의 산술이다.

한계를 기억 능력의 문제로 서술한 것이 정확하지 않은 이유가 여기에 있다.
기억 능력이라고 쓰면 더 큰 모델이면 나아질 것 같지만, 창이 1024인 한 파라미터를 열 배 늘려도 같은 자리에서 잘린다.
캐릭터가 유지되는 것도 같은 산술의 다른 면이다.
말투는 최근 몇 턴만 봐도 이어지지만 사실은 앞쪽에 있었고, 앞쪽이 잘린 것이다.
모델 카드가 창 크기를 모델 정보 항목에 한 줄로만 적고 두 주장과 연결하지 않은 것은 중요한 맥락을 빠뜨린 것이다.

### 온디바이스를 내세우면서 설정은 온디바이스를 향해 있지 않다

이 프로젝트의 가장 눈에 띄는 홍보 문구가 Pi Zero 2W와 갤럭시 워치 4에서 동작한다는 것이고,
이론상 256MB RAM과 CPU만 있으면 구동 가능하다는 것이다.

그런데 설정을 보면 작은 기기를 위한 최적화가 거의 들어 있지 않다.
키·밸류 헤드가 어텐션 헤드와 같은 16개여서 그룹 쿼리 어텐션이나 멀티 쿼리 어텐션을 쓰지 않는다.
컨텍스트를 가득 채웠을 때의 KV 캐시는 레이어 16개에 키와 밸류 각각 16헤드 64차원으로,
토큰당 32,768개 값이고 1,024 토큰이면 약 3,350만 개, 16비트로 잡아도 약 67MB다.
256MB 예산에서 캐시만으로 4분의 1을 쓴다는 뜻이며, 키·밸류 헤드를 4개로 줄였다면 약 17MB로 내려갔을 자리다.

허브에 올라간 가중치가 float32라는 점도 같은 방향이다.
254.7M 파라미터를 4바이트로 저장하면 약 1GB이므로, 256MB 주장은 전적으로 양자화를 전제한다.
모델 카드가 양자화 기준이라고 괄호로 밝히기는 하지만, 어떤 양자화에서 몇 MB가 되는지는 적지 않는다.
GGUF 파일이 함께 올라와 있으므로 그 파일 크기와 실제 상주 메모리를 표로 제시하는 편이
이론상이라는 단서보다 훨씬 강한 근거가 되었을 것이다.

### 캐릭터 유지력이라는 강점만 측정되지 않은 채 남아 있다

모델 카드는 예시 개수에 따른 캐릭터 안정성을 단계별로 제시한다.
30개면 말투와 성격이 잡히고, 50개를 넘기면서 안정되며, 1000개 내외면 사람과 대화하는 것처럼 느껴진다는 것이다.
그리고 같은 조건에서 2배 큰 모델들이 같은 문장을 반복하며 무너지는 동안에도 캐릭터를 유지했다고 적는다.

이 비교가 이 프로젝트의 유일한 성능 주장인데 근거가 하나도 붙어 있지 않다.
2배 큰 모델이 무엇인지, 어떤 캐릭터로 몇 번 시험했는지, 무너졌다는 판정을 누가 어떤 기준으로 했는지가 없다.
캐릭터 유지라는 개념 자체가 정량화하기 어렵다는 점은 사실이지만, 그래서 더 절차가 필요하다.
같은 프롬프트 열 개를 두 모델에 각각 다섯 번씩 넣고 출력을 나란히 공개하는 것만으로도 독자가 직접 판단할 수 있다.

반복 문장으로 무너진다는 서술도 설정과 함께 읽으면 다르게 보인다.
권장 설정이 temperature 0.3~0.5에 repeat_penalty 1.3인데, 이것은 반복을 억제하기 위해 상당히 세게 조인 값이다.
비교 대상 모델에 같은 설정을 적용했는지가 적혀 있지 않으면,
관찰된 차이가 모델의 성질에서 온 것인지 표본화 설정에서 온 것인지 가를 수 없다.

### Ollama 호환 문제를 사용자 쪽 우회로 넘긴다

모델 카드와 게시글 모두 Ollama에서 깨진다는 사실과 원인 진단을 먼저 적고, llama.cpp로 실행하라는 우회를 제시한다.
그리고 작성자는 해결 방법에 대한 의견을 구한다고 덧붙인다.

문제를 숨기지 않은 것은 좋지만, 이 진단이 맞다면 해결의 상당 부분이 모델 쪽에 있다.
Ollama가 pre-tokenizer를 gpt-2 폴백으로 처리하는 이유는 GGUF 메타데이터에 적힌 토크나이저 종류를 알아보지 못했기 때문이고,
그것은 GGUF를 변환할 때 pre-tokenizer 식별자를 어떻게 넣었는지의 문제다.
어휘를 처음부터 만든 모델이라면 기존 식별자 중 무엇과 호환되는지 밝히거나,
정규화와 분리 규칙이 어느 기성 방식과 같은지를 명시해 주는 것이 첫 단계다.

한 걸음 더 나아가면 선택지도 있다.
널리 지원되는 pre-tokenizer 규칙에 맞춰 어휘를 다시 빌드하거나, 변환 스크립트를 함께 배포하거나,
최소한 깨지는 출력과 정상 출력의 예시를 나란히 올려 사용자가 자기 환경이 어느 쪽인지 바로 알게 하는 것이다.
목표 사용자가 초보자인 프로젝트에서 실행 경로의 선택을 사용자에게 넘기면,
모델이 나빠서 이상한 답이 나온다고 오해하고 떠나는 사용자가 생긴다.

## 인사이트

### 작은 모델의 시장은 성능이 아니라 반복 주기에서 열린다

이 프로젝트가 내세운 수치 중 가장 실용적인 것은 파라미터 수도 메모리도 아니라 1~2분이다.
캐릭터 하나를 5060 Ti 16GB에서 LoRA로 학습하는 데 걸리는 시간이며,
그래서 데이터를 고치고 다시 학습해 결과를 확인하는 일을 하루에도 여러 번 할 수 있다는 것이다.

이 수치가 왜 중요한지는 반대편을 생각하면 보인다.
큰 모델에서 같은 실험이 몇 시간 걸리면 하루에 한두 번밖에 돌릴 수 없고,
그러면 데이터를 고치는 판단이 관찰이 아니라 추측에 기대게 된다.
학습이 2분이면 가설을 세우고 확인하는 일이 대화에 가까워지고,
데이터가 결과를 어떻게 바꾸는지에 대한 감각이 실제로 쌓인다.

여기서 작은 모델의 쓸모가 성능 경쟁과 다른 축에 있다는 점이 드러난다.
큰 모델이 못 하는 일을 하는 것이 아니라, 큰 모델로는 할 수 없는 횟수의 시도를 가능하게 하는 것이다.
교육용 도구로서의 가치가 거기에 있고, 데이터셋을 다듬는 실무에서도 같은 논리가 성립한다.
최종 배포를 큰 모델로 하더라도, 어떤 데이터가 어떤 행동을 만드는지를 알아내는 단계는 작은 모델에서 훨씬 싸게 끝난다.

### 사실을 못 외우는 모델이 오히려 안전한 용도가 있다

세계 지식이 약하고 영어와 수학을 못한다는 것은 이 모델의 한계로 적혀 있지만,
캐릭터 챗이라는 용도에서는 성질이 달라진다.

큰 모델로 캐릭터 챗을 만들 때 반복해서 겪는 문제가 캐릭터가 아는 것이 너무 많다는 점이다.
중세 배경의 인물이 상대성 이론을 설명하고, 기억을 잃은 설정의 존재가 최신 뉴스를 안다.
설정을 지키게 하려면 아는 것을 모른 척하도록 프롬프트로 억누르는 작업이 필요하고, 그 억제는 대화가 길어질수록 새어 나간다.
지식이 애초에 없는 모델에서는 이 문제가 발생하지 않는다.

같은 이유로 안전 측면의 성질도 달라진다.
사실을 그럴듯하게 지어내 사용자가 믿게 만드는 위험은 모델이 사실을 말하는 자리에 놓일 때 생기는데,
이 모델은 그 자리에 놓이기에는 너무 명백하게 모른다.
예시 캐릭터 중 하나가 자기 이름과 수학·영어를 못한다는 한계를 스스로 인정하도록 만들어진 것도 같은 설계 감각이다.

그래서 능력의 부재를 용도의 조건으로 바꾸는 선택이 일반적인 패턴으로 남는다.
모델을 고를 때 무엇을 할 수 있는지만 보지 않고 무엇을 하지 못해야 하는지를 함께 보는 것이며,
후자가 요구 사항인 자리가 생각보다 많다.

### 처음부터 만드는 일의 비용은 학습이 아니라 생태계에서 나온다

이 프로젝트에서 가장 값비싼 대가는 6.4B 토큰을 돌린 계산 비용이 아니라 Ollama에서 깨진다는 한 줄이다.

파인튜닝을 택했다면 기존 어휘와 토크나이저와 채팅 템플릿을 그대로 물려받고,
그 모델을 이미 지원하는 런타임과 양자화 도구와 서빙 스택이 전부 따라온다.
처음부터 만들면 그 모든 접점을 새로 검증해야 하고, 검증하지 못한 접점에서 조용히 깨진다.
어휘를 직접 만든 모델이 기성 런타임의 폴백 경로에 걸린 이 사례가 정확히 그 유형이다.

이 구분은 다른 영역에서도 반복된다.
자체 프로토콜, 자체 설정 형식, 자체 패키지 형식을 만들 때 드는 비용은 만드는 시간이 아니라
그 이후 모든 도구와의 접점에서 발생하는 마찰의 총합이고, 그 총합은 처음에 보이지 않는다.
공개 시점에 기능은 완성되어 있는데 채택이 안 되는 프로젝트의 상당수가 여기에 걸려 있다.

그래서 처음부터 만드는 선택을 할 때 함께 정해야 할 것이 있다.
어느 접점까지는 기성 규격을 그대로 따를 것인지다.
모델이라면 어휘는 새로 만들더라도 pre-tokenizer 규칙과 채팅 템플릿은 널리 쓰이는 형식과 일치시키는 식이다.
배우는 것이 목적이라면 전부 직접 만드는 편이 낫지만, 쓰이는 것이 목적이라면 접점은 남의 것을 쓰는 편이 거의 항상 낫다.

### 미성년자의 기술 결과물을 평가할 경로가 입시밖에 없다

댓글 두 개가 오간 방향은 사소해 보이지만 구조적인 문제를 보여 준다.
결과물을 본 첫 반응이 생활기록부에 쓸 수 있느냐였고, 작성자의 답은 성적 때문에 그 경로가 막혀 있을 것 같다는 것이었다.[^parkjy][^mogodik]

여기서 어긋나 있는 것은 평가 축의 수다.
6.4B 토큰 프리트레인을 완주하고, 학습 설정을 검산 가능하게 공개하고,
자기 모델의 실행 환경 버그를 진단해 함께 적어 둔 사람에게 열려 있는 경로가 입시 하나뿐이라면,
그 경로에서 걸러진 능력은 어디에도 기록되지 않는다.

성인 개발자에게는 이런 결과물을 평가할 경로가 여럿 있다.
채용, 오픈소스 커뮤니티에서의 평판, 컨퍼런스 발표, 기술 블로그의 구독자가 그것이다.
미성년자에게는 대부분이 닫혀 있거나 접근하기 어렵고,
남은 것이 학교라는 단일 창구이며 그 창구는 성적이라는 다른 축과 묶여 있다.

이 공백을 메우는 장치가 비싼 것도 아니다.
고등학생이 참가할 수 있는 기술 대회, 멘토가 붙는 오픈소스 프로그램,
결과물만으로 평가하는 인턴 경로가 그런 장치이고 이미 다른 나라에는 여럿 있다.
작성자가 포트폴리오라는 단어를 스스로 꺼냈다는 사실이 시사적이다.
평가받을 경로가 없다는 것을 알면서 언젠가 평가받을 것을 가정하고 만들어 둔 것이며,
그 가정이 실제로 작동하는지는 이 학생이 아니라 이 분야가 답할 문제다.

---

[^parkjy]: <https://news.hada.io/topic?id=33581#cid65376>

[^mogodik]: <https://news.hada.io/topic?id=33581#cid65394>
