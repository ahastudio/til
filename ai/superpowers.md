# Superpowers

<https://github.com/obra/superpowers>

## 이게 뭔가

코딩 에이전트(Claude Code, Cursor, Codex 등)에 얹는
워크플로우 프레임워크다. 조합 가능한 스킬 모음으로
브레인스토밍, 계획, 서브에이전트 구현, TDD,
코드 리뷰, 브랜치 관리까지 7단계를 제공한다.

설치하면 에이전트의 작업 방식 자체가 바뀐다.
"코드 짜줘"가 아니라 "설계하고, 승인받고, 계획 세우고,
구현하고, 테스트하고, 리뷰받고, 머지해라"가 된다.

## 인사이트

### 에이전트의 가장 위험한 본능을 꺾는다

에이전트는 코드부터 짠다. 무조건.
질문 안 한다. 설계 안 한다. 확인 안 한다.
"간단해 보여서"라는 이유로 바로 구현에 돌입하고,
500줄을 쏟아낸 뒤에야 방향이 틀렸다는 걸 안다.

Superpowers의 브레인스토밍 스킬은
이 충동을 **물리적으로 차단**한다.
설계 문서를 제출하고 사용자가 승인하기 전까지
코드를 한 줄도 못 쓴다. 예외 없다.
"간단한 프로젝트"도 없다. 게이트가 열리지 않는다.

이건 가이드라인이 아니다. **강제**다.
CLAUDE.md에 "설계 먼저 해"라고 써봐야
에이전트는 교묘하게 우회한다.
Superpowers는 우회 자체가 불가능한 구조를 만든다.

### 기억이 오염되면 에이전트는 망가진다

에이전트 하나가 작업 5개를 연속으로 하면
세 번째쯤부터 이상해진다.
첫 번째 작업의 변수명을 네 번째에서 쓰고,
두 번째에서 삭제한 함수를 다섯 번째에서 호출한다.
컨텍스트가 서로 스며드는 것이다.

Superpowers는 **작업마다 에이전트를 새로 만든다.**
이전 작업의 기억이 제로다. 오염 자체가 불가능하다.

그리고 이 새 에이전트는 코딩부터 하지 않는다.
**질문부터 한다.** 뭘 모르는지 먼저 묻는다.
구현 전에 모호함을 표면화시킨다.
잘못된 가정 위에 코드를 쌓는 재앙을 원천 차단한다.

### 테스트를 "작성"하는 것과 "목격"하는 것은 다르다

대부분의 팀이 TDD를 한다고 말하면서
실제로는 코드를 먼저 쓰고 테스트를 나중에 붙인다.
나중에 붙인 테스트는 당연히 통과한다.
**하지만 그 테스트가 진짜 올바른 동작을 검증하는지는
아무도 모른다.**

Superpowers의 TDD 스킬은 테스트가 **실패하는 것을
직접 목격**하도록 강제한다.
실패를 보지 못한 테스트는 테스트가 아니다.

여기서 끝이 아니다. 가장 잔인한 규칙이 있다.
**실패 테스트 없이 먼저 작성된 프로덕션 코드는
전부 삭제한다.** 100줄이든 1000줄이든 관계없다.
"아까우니까 살려두자"는 매몰 비용 오류다.
이 규칙이 그 오류를 구조적으로 불가능하게 만든다.

### 리뷰를 한 번에 하면 반드시 놓친다

코드 리뷰에서 "이거 잘 됐나?" 하고 한 번 보면
두 가지가 뒤섞인다.
요구사항을 충족하는가? 구현이 깨끗한가?
하나에 집중하면 다른 하나를 놓친다.
그래서 "동작하지만 더러운 코드"가 머지되고,
"깨끗하지만 스펙에서 벗어난 코드"가 머지된다.

Superpowers는 리뷰를 **두 단계로 분리**한다.
스펙 준수 리뷰가 먼저다. 요구사항만 본다.
코드 품질 리뷰가 다음이다. 구현만 본다.
한 번에 하나만 보니까 놓칠 수가 없다.

### 자율성의 올바른 단위는 "3개"다

에이전트에게 작업 20개를 주고 돌아서면
10개째에서 방향이 틀어져 있어도 모른다.
20개가 끝나고 나서야 발견하면
그때는 이미 10개를 버려야 한다.

Superpowers는 **3개마다 사람이 확인**한다.
블로커를 만나면 추측하지 않고 즉시 멈춘다.
에이전트가 충분히 일하되,
돌이킬 수 없을 만큼 멀리 가지는 못하게 한다.

완전한 자율도 아니고 완전한 통제도 아닌,
**3개라는 단위가 최적의 균형점**이라는 것.
이것이 Superpowers가 실전에서 찾아낸 숫자다.

## 관련 문서

- [Spec-Driven Development](./spec-driven-development.md)
- [File-based Planning Workflow](./file-based-planning-workflow.md)
- [Karpathy's Claude Coding Notes](./karpathy-claude-coding-notes.md)
